import { css } from 'lit-element';

export default css`
  :host {
    display: block;
    padding: var(--spacing-3x);
    text-align: center;
    min-width: 350px;
    max-width: 540px;
    margin: 0 auto;
  }
  img {
    width: 250px;
  }
`;
